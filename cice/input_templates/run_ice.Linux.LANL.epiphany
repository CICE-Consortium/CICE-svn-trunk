#PBS -N cice
#PBS -l nodes=4
#PBS -l mem=2G
#PBS -l walltime=00:30:00
#PBS -j oe
#PBS -m e

setenv SYSTEM_USERDIR /scratch1/eclare

set  stamp   = `date -u '+%y%m%d%H%M%S'`
setenv ICE_LOG_FILE ice.log.${stamp}

cd $SYSTEM_USERDIR/rundir
mpirun -np 8 -c core=0 ./cice >&! $ICE_LOG_FILE

# the old way:
# mpirun -v -machinefile NODES33-36 -nolocal -np 8 cice >&! $ICE_LOG_FILE
